name = "p31-pwa"
compatibility_date = "2024-09-25"
pages_build_output_dir = "dist"

# No [build] — build runs locally or in dashboard (Git). Deploy: npm run build && npx wrangler pages deploy dist --project-name p31-pwa
